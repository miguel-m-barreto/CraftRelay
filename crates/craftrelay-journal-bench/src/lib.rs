#![forbid(unsafe_code)]

//! Sprint 3 journal benchmark spike. NOT a production journal.
//! Produces synthetic benchmark data only — no DurableReceipt, no real acceptance.

pub mod candidate;
pub mod ingress;
pub mod report;
